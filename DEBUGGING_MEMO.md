# SWELL デバッグメモ - 同じミスを繰り返さないために

## 発見されたデグレード問題

### 1. 矢印キーの無反応問題（重大）→ ✅ 修正済み (2026-04-14)
**症状:**
- 矢印ボタンのクリックが無反応
- スライド自体は反応しているが、ページ数表示（ページインジケータ）がカード枚数に追従していない

**根本原因（2026-04-14 特定）:**
- `components/pixel-arrow.tsx` の **native 分岐**(`Platform.OS !== 'web'`) が `<View>` + `onTouchStart`/`onTouchEnd` のみで **`onPress` を呼んでいなかった**
- Web 分岐は `<button onClick>` で正しく動作していたが、iOS/Android 実機で矢印が無反応
- 過去の `Pressable → TouchableOpacity` など複数の試行錯誤の痕跡が残っていたが、native 分岐の実装だけ取り残されていた

**修正内容:**
- native 分岐を `<Pressable onPress={handlePress}>` に変更
- `onPressIn`/`onPressOut` で `isPressed` 状態を管理
- `hitSlop={8}` を付与してタップ領域を拡張
- Codex CLI による独立監査で発見されたバグ

**教訓:**
- Web と Native で分岐するコンポーネントは、両方の分岐を必ずテストする
- `Pressable` import してあるのに使っていない場合は疑う

---

### 2. YouTubeサムネイル取得失敗（重大）
**症状:**
- YouTubeサムネイルが表示されない
- クリックで動画にも飛べない

**根本原因の推測:**
- YouTube API からサムネイル URL が取得できていない
- サムネイル URL は取得されているが、画像読み込みに失敗している（CORS エラー）
- ビデオコンポーネントのクリックハンドラが実装されていない

**修正方法（次回実装時）:**
1. `VideoClient` でサムネイル URL が正しく取得されているか確認
2. ブラウザのコンソールで CORS エラーを確認
3. サムネイル画像の読み込み状態をデバッグ
4. ビデオアイテムのクリックハンドラを実装（YouTube へのディープリンク）

**チェックリスト:**
- [ ] `server/clients/video-client.ts` でサムネイル URL 取得ロジックを確認
- [ ] ブラウザコンソールで CORS エラーを確認
- [ ] `Image` コンポーネントの `onError` ハンドラでエラーをログ出力
- [ ] ビデオアイテムに `onPress` ハンドラを追加（YouTube へのリンク）
- [ ] テストデータでサムネイル表示を検証

---

### 3. 株価チャートデザイン未実装（重大）
**症状:**
- 株価セクションのデザインがまだ導入されていない
- チャートのようなデザインを波で表現する必要がある

**要件:**
- 株価データを波のビジュアルで表現
- 上昇/下降を波の高さで表現
- SWELL アプリのテーマに合わせたデザイン

**修正方法（次回実装時）:**
1. 株価データ（高値、安値、終値）を取得
2. 波のビジュアルコンポーネントを作成（`WaveChart.tsx`）
3. 株価データを波の高さにマッピング
4. 色分け（上昇=緑、下降=赤）を実装

**チェックリスト:**
- [ ] `server/clients/stock-client.ts` で株価データ構造を確認
- [ ] `components/wave-chart.tsx` を新規作成
- [ ] 波のアニメーション実装（`react-native-reanimated`）
- [ ] 株価データのマッピングロジック実装
- [ ] 色分け（上昇/下降）を実装
- [ ] テストデータで波チャート表示を検証

---

## 教訓と今後の対策

### 1. テスト駆動開発の重要性
**教訓:** プログラマティッククリックテストだけでは不十分。ユーザーの手動クリックでも動作するか確認が必須。

**対策:**
- 状態管理の変化をテストする Vitest テストを必ず書く
- UI コンポーネントの統合テストを実装
- ブラウザでの手動テストを必ず実施

### 2. 外部 API 連携の検証
**教訓:** API から取得したデータが正しく表示されているか、ブラウザのコンソールで CORS エラーなどを確認する必要がある。

**対策:**
- API レスポンスをログ出力
- ブラウザコンソールのエラーを常に監視
- 画像読み込みエラーハンドリングを実装
- テストデータで動作確認

### 3. デザイン実装の完全性
**教訓:** デザイン要件が「波で表現する」など具体的でない場合、実装漏れが発生しやすい。

**対策:**
- デザイン要件を具体的に定義（色、サイズ、アニメーション）
- デザイン実装をチェックリスト化
- 各セクション（News、Social、Market）のデザインを統一

### 4. チェックポイント作成前の検証
**教訓:** プログラマティッククリックテストで成功しても、実際のユーザー操作では失敗することがある。

**対策:**
- チェックポイント作成前に、すべての主要機能を手動でテスト
- 各セクション（News、Social、Market）でページネーション動作確認
- 外部リンク（YouTube、ニュース元）のクリック動作確認
- ブラウザのコンソールエラーを確認

---

## 修正優先度

1. **高優先度（即座に修正）:**
   - 矢印キーの無反応問題
   - YouTubeサムネイル取得失敗
   - 株価チャートデザイン実装

2. **中優先度（次のイテレーション）:**
   - エラーハンドリング改善
   - ローディング状態の表示
   - キャッシング戦略の最適化

3. **低優先度（将来の改善）:**
   - パフォーマンス最適化
   - アニメーション追加
   - ユーザー設定機能

---

## チェックリスト（修正実装時に使用）

- [ ] 矢印ボタンのクリックハンドラが状態を正しく更新しているか確認
- [ ] ページインジケータが最新の状態を表示しているか確認
- [ ] YouTubeサムネイル URL が正しく取得されているか確認
- [ ] サムネイル画像の読み込みエラーをハンドリング
- [ ] ビデオアイテムのクリックで YouTube へ遷移するか確認
- [ ] 株価チャートコンポーネントを実装
- [ ] 波のビジュアルで株価データを表現
- [ ] 上昇/下降の色分けを実装
- [ ] ブラウザコンソールでエラーがないか確認
- [ ] すべての主要機能を手動でテスト
- [ ] チェックポイント作成前に完全なテストを実施
